spring:
  application:
    name: chess
  mvc:
    pathmatch:
      matching-strategy: ANT_PATH_MATCHER
  datasource:
    url: jdbc:mariadb://${DB_CHESS}/chess
    password: root
    username: root

debug: off
