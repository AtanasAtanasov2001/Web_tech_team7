spring:
  application:
    name: chess
  mvc:
    pathmatch:
      matching-strategy: ANT_PATH_MATCHER
  datasource:
    url: jdbc:mariadb://db_chess:3306/chess
    password: root
    username: root

debug: off
